version: 2
jobs:
  build:
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run:
          name: Build and push Docker image
          command: chmod +x ./build.sh && ./build.sh